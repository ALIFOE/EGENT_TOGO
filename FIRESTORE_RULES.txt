rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ✅ DONNÉES PUBLIQUES - Lecture seule, édition admin uniquement
    match /products/{document=**} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }
    match /articles/{document=**} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }
    match /gallery/{document=**} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }
    match /projects/{document=**} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }
    
    // 🖼️ IMAGES DES ARTICLES
    match /articleImages/{document=**} {
      allow read: if true;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null;
    }
    
    // ⚠️ FORMULAIRES DE CONTACT - Public peut créer, admin modère
    match /contact_forms/{document=**} {
      allow create: if true;
      allow read, update, delete: if request.auth != null;
    }
    
    // ⚠️ DEVIS - PUBLIC PEUT CRÉER SANS RESTRICTION
    // Ultra-permissif pour déboguer
    match /quotes/{document=**} {
      allow create, read, write, delete: if true;
    }
  }
}
